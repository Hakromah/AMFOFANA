package com.amfofana.school;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = BackendApplication.class) // Explicitly tell the test where the config is
class BackendApplicationTests {

    @Test
    void contextLoads() {
    }

}
